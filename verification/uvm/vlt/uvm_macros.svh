// UVM macro include shim for the open-source (Verilator) UVM flow.
// The monolithic UVM header (first on the command line) defines every `uvm_*
// macro; this stub just satisfies the sources `include "uvm_macros.svh".
// (First comment word avoids "verilator", parsed as a pragma.)
